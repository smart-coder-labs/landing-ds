/* ========================================
   QR CODE GENERATOR - TYPES
   ======================================== */

export interface QRCodeGeneratorProps {
    defaultValue?: string;
    size?: number;
    color?: string;
    backgroundColor?: string;
    showInput?: boolean;
    className?: string;
}